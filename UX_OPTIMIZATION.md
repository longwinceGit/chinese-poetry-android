# 「诗词乐园」UX 优化清单

> 由 UX 架构师对 `fragment_home / fragment_detail / fragment_profile / fragment_learning / fragment_game_hub / item_poem_card / nav_graph.xml` 及 Home/Detail/Profile/MatchGame/StatsBarChart 等源码**逐一核验**后输出。
>
> 每条按「问题 → 用户影响 → 方案 → 工作量 → 优先级」组织，全部可在现有技术栈（Android View System + Canvas + Material 3）内落地，不引入重型依赖或后端。

---

## P0 · 高价值 / 低成本（3-7 天工程投入，体验收益最大）

### P0-1　"已学"状态缺闭环引导，任务完成无查看入口

**问题**：`detail_learn`（标记已学）/ `detail_learned` 均被禁用/置灰，但首页/详情页与"我的→已学诗词"之间没有引导闭环；用户在学习页点"学一首诗"任务后，不知道去哪里看进度。
**用户影响**：任务闭环断裂——"每日任务→学一首诗"完成了，但看不到累计成果，激励感打折。
**方案**：在 DetailFragment 的"已学"按钮（`alpha 0.6` 置灰态）下方加一行小字"已计入学习统计，可在 我的→已学诗词 查看"，点按可跳转已学 tab。同时把置灰态改为"可再次点击但显示确认提示"。
**工作量**：S（纯 UI + 一条跳转）

### P0-2　「我的」页成就/主题只展示前 6 个，9 主题 / 12 成就无法看全

**问题**：`ProfileFragment.buildAchievements()` 与 `buildThemes()` 都 `Math.min(6, all.size())`——12 个成就只显示 6 个，9 套主题只显示 6 套，且**没有"查看全部"入口**。
**用户影响**：成长体系的"目标感"被截断——用户不知道自己还有哪些成就可以解锁、还有哪些主题皮肤等着他，这是激励系统的致命缺口。
**方案**：① 新增"查看全部 →"入口（`nav_all_achievements` 二级页）；② 或改为横向滑动容器显示全部；③ 或直接全部渲染（改 `Math.min(6, ...)`）。
**工作量**：S~M

### P0-3　`profile_settings` / `profile_about` 字符串已存在但功能缺失

**问题**：`strings.xml` 定义了 `profile_settings`（设置）和 `profile_about`（关于），但 nav_graph 中没有任何 settings/about 目的地，Profile 页也没有这两项。README 声称有"**减少动画**开关"，但代码 grep 不到任何 reduce/animat 偏好设置逻辑。
**用户影响**：① "减少动画"是一个**宣称了但没有实现**的无障碍承诺（与 README 不符）；② 深色模式只能跟随系统，App 内无手动设置。
**方案**：在 Profile 底部加"设置"入口（`nav_settings` 二级页），至少实现：**减少动画开关**（SharedPreferences 存储，PoemAdapter 回弹 / ConfettiView / 消除动画按开关跳过）和深色/跟随系统主题选择（复用已有 DayNight 主题与 `values-night/`）。删除或实现 `profile_about`。
**工作量**：M

### P0-4　冷启动 3-5s 全量加载 91K 首，首屏只有转圈

**问题**：`HomeFragment.loadPoems()` 一次性解析 54MB / 91,196 首（3-5s、内存 ~150MB）。首页需等全部数据加载完才显示网格，此前只有 `loading_container` 转圈 + "墨香氤氲，诗词正在加载……"。
**用户影响**：首次进入等待 3-5 秒且无骨架屏，流失率最高。
**方案**：分阶段加载——① 先解析 500 首著名（有释义）诗词快速填充首屏；② 再后台全量加载其余；或至少：加载时先显示每日推荐卡片 + 分类 Chips，网格在数据就绪后再出现。配合骨架屏（灰块 shimmer）替代纯转圈。
**工作量**：M
**注**：与 README「后续优化方向 ①」一致，属既有规划落地。

### P0-5　StatsBarChart（7 日趋势图）完全无无障碍描述

**问题**：`StatsBarChart.onDraw()` 直接 Canvas 绘制柱状图和数值，但该 View **没有设置任何 contentDescription 或标签**，也没有 `onInitializeAccessibilityEvent`。学习页最核心的数据可视化对 TalkBack 用户完全"黑屏"。
**用户影响**：视障用户在"学习"tab 无法获知连续学习天数趋势。
**方案**：在 `StatsBarChart.setData()` 内为每个 bar 的数值构建聚合文本（如"周一共学 3 首"）拼成 contentDescription，并在 `invalidate` 时更新；同时给外层 `card_chart` 加 `android:labelFor` / `contentDescription`。
**工作量**：S

---

## P1 · 高价值 / 中成本（能明显提升留存与评分）

### P1-1　首页"加载更多"按钮 → 无限滚动

**问题**：首页用显式"查看更多"按钮（`btn_load_more`），点一下 `adapter.setPoems()` → 全量重算。91K 首里用户要连点几十次才可能触达下半区；分类 Chip 在窄屏上需横向滑动（`HorizontalScrollView`），无溢出提示。
**用户影响**：长列表浏览连续性被打断，"查看更多"每点一次才 +30，探索长尾诗词意愿低。
**方案**：改为 RecyclerView 底部自动加载（`GridLayoutManager` + 滚到底触发 `loadMore()`），保留按钮作为兜底（内容 < 可见高度时）。同时给分类栏加渐隐遮罩提示"可横向滑动"。
**工作量**：S~M

### P1-2　诗词卡片 emoji 为 `index % 10` 循环分配，视觉信息无意义

**问题**：`PoemLoader` 计算 `emoji = index % 10`（每 10 首循环同一组 emoji），`PoemAdapter` 把 emoji 作为卡片主视觉。同一朝代、不同题材的诗词可能撞同一个图标。
**用户影响**：emoji 无法帮助用户区分/记忆诗词，反而造成视觉噪音；部分 Android 设备缺少字体渲染时显示为方框（豆腐块）。
**方案**：用朝代专属的矢量图标或统一水墨印章样式替换分散的 emoji（如唐代一律用"唐"字印章、宋代用"宋"），既减噪音又强化朝代识别。新增 11 个朝代印章 drawable。
**工作量**：M

### P1-3　详情页 6 个操作布局：拼音按钮与主操作栏割裂

**问题**：`fragment_detail.xml` 把"拼音"做成独立 text-only 小按钮（`btn_pinyin`），与底部 收藏/朗读/分享/已学 栏分离；拼音切换无状态提示；TTS 用"朗读/暂停"往返切换，暂停后无进度提示。
**用户影响**：用户第一次看到详情页，不清楚"拼音"是干嘛的；TTS 朗读中无法跳转某一行或调速。
**方案**：① 把"拼音"合并进底部一级操作区，用 toggle MaterialButton + 图标；② TTS 按钮增加朗读中态（icon 变 pause + 高亮）。
**工作量**：S~M

### P1-4　空状态已有提示文案但缺行动按钮

**问题**：收藏/已学列表已有空状态文案（"你还没有收藏任何诗词"/"去详情页点收藏"），但**没有行动按钮**直达详情页/首页。
**用户影响**：新手点进"我的收藏"看到空态不知道该去哪操作。
**方案**：空状态下方加"去首页浏览"主按钮（navigate 到 nav_home）和"去学习"次按钮。
**工作量**：S

### P1-5　成就解锁庆祝错过即不可回顾

**问题**：成就解锁走 `Toast + ConfettiView.celebrate()`，LiveData 消费后清除。若用户恰好在别的页面或没留意到 Toast，**解锁过的成就在"我的"页无任何高亮**。
**用户影响**：激励反馈不可回溯，用户不知道"我获得了什么成就、什么时候获得的"。
**方案**：已解锁成就项加"NEW"角标（记录最近解锁时间），或刚解锁的成就在列表顶部短暂高亮一次。
**工作量**：S

### P1-6　「每日推荐」数据固定但无"换一首"入口

**问题**：每日推荐基于日期哈希固定（同一天同一首），但 88 首著名诗词里只抽 1 首。
**用户影响**：对诗词不感兴趣的用户当天没有替代选择。
**方案**：每日卡片加"换一首"刷新图标（从 famousPoems 随机取，本地实现）。
**工作量**：S

---

## P2 · 中价值 / 中低成本（打磨与一致性问题）

### P2-1　古风美学在 Material 3 下的表达：emoji 图标 vs 矢量统一

**问题**：全 App 视觉在 emoji 与矢量图标间摇摆——头像、占位、成就、主题、底部标签、游戏入口大量用 emoji，而操作按钮用 `ic_*` 矢量。9 套主题切换后可能只是换色板，无预览。
**用户影响**：视觉语言不统一，古风（墨色/印章/宣纸）美学被 emoji 的"手机默认感"冲淡。
**方案**：① 关键品牌位（底部 4 tab 图标、收藏/分享/朗读按钮）统一为水墨风矢量；② 主题选择卡片增加色板预览（Canvas 画 3-4 个色块）。
**工作量**：L（图标重绘 + 预览 drawable，可拆分做）

### P2-2　详情页分享卡与结果页样式走查

**问题**：`sharePoem()` 异步生成 PNG + FileProvider 分享，但点击"分享"到系统分享面板之间**无任何 loading 反馈**（长诗 OOM 时只 Toast"墨未干"）。分享卡样式由 Canvas 绘制，无法在 App 内预览。
**用户影响**：用户无提示等待 1-2 秒，分享前无法预览卡片效果。
**方案**：分享按钮点击后短暂禁用并显示"生成中…"；可选地，长按分享卡片弹出预览 Dialog。
**工作量**：S~M

### P2-3　游戏页反馈叠加 + 结果页信息不完整

**问题**：消消乐完成时 `Toast + 完成弹层` **双重反馈**；完成弹层只显示"得分/尝试次数"，没有"再战一局/回到大厅"的结构化信息与难度说明。
**用户影响**：完成反馈冗余，缺少难度梯度或"错题回顾"。
**方案**：① 消消乐完成去掉 Toast，只留弹层（加"回大厅"按钮）；② 对诗/填空错误项显示正确答案后提供"重玩本局"；③ 填空增加简单/中等/困难三档挖空量。
**工作量**：M

### P2-4　学习页信息密度大、图表缺图例

**问题**：`fragment_learning` 一屏塞下 4 个卡片（统计卡 → 趋势图 → 打卡日历 → 每日任务），但趋势图与日历都没有图例说明。
**用户影响**：信息密度高，新用户第一眼不知道重点；柱状图无量纲标注。
**方案**：图表加说明文案或点击柱子弹值；每日任务可点击直达动作（学两首、答一题、玩一局分别 deep-link 到 detail/game）。
**工作量**：S~M

---

## P3 · 长期 / 储备（可进 backlog，避免当下投入）

### P3-1　搜索体验增强：最近搜索 / 空结果纠正

**问题**：`et_search` 500ms 防抖 + 倒排索引已很快，但无"最近搜索"建议，空结果只有"未找到，换个关键词再试试"。
**用户影响**：常见输入法/笔误无法被纠正，长尾检索成本高。
**方案（纯本地）**：维护最近 10 条搜索历史（Room/SharedPrefs），输入聚焦时展示；空结果做朴素拼音前缀匹配纠正（复用 `PinyinHelper`）。
**工作量**：M

### P3-2　详情页拼音/朗读的进阶：逐句 / 调速

**问题**：TTS 固定 0.85x 且只能全文朗读/暂停，无法只读某一句或调速。
**方案**：朗读支持"下一句/上一句"按钮，或长按朗读弹"0.7x / 0.85x / 1.0x"选择（`TtsManager` 已有语速参数）。
**工作量**：M

### P3-3　数据自愈 `repairRecords()` 的可观测与降级

**问题**：收藏/已学旧记录自动补齐逻辑很稳，但若 `PoemRepository` 尚未完全加载（首屏仅 500 首），`findPoemById()` 可能查不到导致补不齐。
**方案**：repair 阶段等全量数据就绪后再跑，或对查不到的记录保留原样并跳过。
**工作量**：S

---

## 汇总表

| 编号 | 条目 | 优先级 | 工作量 | 主要影响维度 |
|------|------|--------|--------|--------------|
| P0-1 | 已学状态闭环与返回引导 | P0 | S | 学习路径 |
| P0-2 | 成就/主题只显示 6 个无全览 | P0 | S~M | 激励体系 |
| P0-3 | 设置/减少动画缺失（README 承诺未落地） | P0 | M | 反馈/无障碍/信任 |
| P0-4 | 冷启动全量加载 3-5s 无骨架屏 | P0 | M | 性能感知 |
| P0-5 | StatsBarChart 无 a11y | P0 | S | 无障碍 |
| P1-1 | 首页"加载更多"→ 无限滚动 | P1 | S~M | 首页体验 |
| P1-2 | 卡片 emoji 循环分配无意义 | P1 | M | 视觉一致性 |
| P1-3 | 详情页拼音按钮与 TTS 割裂 | P1 | S~M | 详情页体验 |
| P1-4 | 收藏/已学空状态无行动按钮 | P1 | S | 空状态 |
| P1-5 | 成就解锁错过不可回顾 | P1 | S | 反馈与激励 |
| P1-6 | 每日推荐无"换一首" | P1 | S | 首页体验 |
| P2-1 | emoji vs 矢量不统一 + 主题预览 | P2 | L | 品牌一致性 |
| P2-2 | 分享无 loading/无法预览 | P2 | S~M | 详情页/反馈 |
| P2-3 | 游戏完成反馈冗余 + 结果信息不全 | P2 | M | 游戏体验 |
| P2-4 | 学习页信息密度大、图表缺图例 | P2 | S~M | 学习路径 |
| P3-1 | 搜索历史与拼音纠错 | P3 | M | 首页体验 |
| P3-2 | TTS 逐句/调速 | P3 | M | 详情页体验 |
| P3-3 | repairRecords 数据就绪时序 | P3 | S | 健壮性 |

---

## 关键结论（务实建议）

1. **最该立刻做的是 P0-2 / P0-3 / P0-5 三个"低成本高感知"项**：它们分别修复了激励系统可见性、README 承诺落空、无障碍硬缺口，各只需 1-2 天。
2. **P0-4（启动分阶段加载）**是 README 里已规划的优化方向①，且能同时缓解内存 150MB 问题——建议排在 P0-2 之后，因为它动到数据加载链路，需回归测试。
3. **无重型依赖红线已遵守**：所有方案（骨架屏、无限滚动、主题预览、成就全览、设置开关）都能在现有 View System + Canvas + Material 3 + Room/SharedPrefs 内实现，无需引入新库或后端。
4. **README 与代码的一致性需要修复**："减少动画"功能是不存在却宣称了的，会直接损害用户信任，务必补上或从文档移除。
