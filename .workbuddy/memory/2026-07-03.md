# 2026-07-03 工作日志

## P0-A 紧急 Bug 修复（已完成）
- PinyinHelper 字符级 LRU 缓存（4096 capacity, accessOrder=true）
- PinyinLineView 延迟构建（mBuilt 标志避免重复 measure）
- DetailFragment 分享卡：ApplicationContext + RGB_565 + OOM 捕获
- ContextCompat.getColor() 兼容 hotfix（16处替换，4文件加 import）
- 编译验证：assembleDebug BUILD SUCCESSFUL in 1m25s
- 产出 APK：21.3 MB

## 搜索性能优化（已完成）
- 搜索移至后台线程（volatile searchCancelled 取消旧搜索）
- PoemAdapter 使用 DiffUtil 替代 notifyDataSetChanged
- 搜索结果分页加载（PAGE_SIZE=30）
- 搜索加载状态指示（ProgressBar）

## 设计令牌系统 v1.0 落地（已完成）
### 新建文件
- `values/dimens.xml`：三层令牌架构核心文件
  - L1 原始令牌：9级间距(2~48dp) + 14级字号(10~48sp) + 3级行高 + 5级圆角 + 4级阴影 + 3级描边 + 13项组件尺寸 + 2级触摸目标 + 3级动效
  - L2 组件令牌：Widget.Poetry.* 样式（Card/Button/Chip/TextAppearance）
  - L3 语义令牌：spacing_page_horizontal / spacing_card_padding 等语义别名

### 修改文件
- `values/colors.xml`：追加 17 个语义色令牌（text_*/bg_*/divider_*/state_*）
- `values/styles.xml`：全量重写，所有硬编码值→@dimen/@color 引用
  - 新增 7 个 TextAppearance（PageTitle/SectionTitle/CardTitle/BodySecondary/Explanation/StatNumber/DailyLabel）
  - 新增 3 个 Card 变体（标准/Raised/Search）+ 3 个 Button 变体 + 2 个 Chip 变体
  - 4 个 ShapeAppearance（Small/Medium/Large/Pill）
- `values-night/styles.xml`：windowLightStatusBar 加 tools:targetApi="23"

### 迁移布局文件（13个全量重写）
- fragment_home / fragment_detail / fragment_learning / fragment_profile
- fragment_favorites / fragment_game_hub / fragment_game_match / fragment_quiz / fragment_game_couplet
- item_poem_card / item_achievement / item_favorite / activity_main
- 所有 textSize/padding/margin/color 硬编码值→令牌引用
- 修复 item_poem_card.xml 的 MissingConstraints lint error

### 验证结果
- `assembleDebug` BUILD SUCCESSFUL in 17s
- `lintDebug` BUILD SUCCESSFUL（0 errors，2个预存 NewApi 已修复）
- 修复方式：windowLightStatusBar 加 `tools:targetApi="23"`（API 21-22 运行时自动忽略）
